import { Http } from '@/utils';
import { Util } from '@/utils';
import PlanTempletDetailServiceBase from './plan-templet-detail-service-base';


/**
 * 计划模板详情服务对象
 *
 * @export
 * @class PlanTempletDetailService
 * @extends {PlanTempletDetailServiceBase}
 */
export default class PlanTempletDetailService extends PlanTempletDetailServiceBase {

    /**
     * Creates an instance of  PlanTempletDetailService.
     * 
     * @param {*} [opts={}]
     * @memberof  PlanTempletDetailService
     */
    constructor(opts: any = {}) {
        super(opts);
    }


}