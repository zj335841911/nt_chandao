import { Http,Util } from '@/utils';
import FixPathLogicBase from './fix-path-logic-base';

/**
 * 重建模块路径
 *
 * @export
 * @class FixPathLogic
 */
export default class FixPathLogic extends FixPathLogicBase{

    /**
     * Creates an instance of  FixPathLogic
     * 
     * @param {*} [opts={}]
     * @memberof  FixPathLogic
     */
    constructor(opts: any = {}) {
        super(opts);
    }

}